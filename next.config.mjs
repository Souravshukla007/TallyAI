/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 16 removed the built-in `next lint` / eslint-during-build
  // integration, so no `eslint` key is needed here. Linting is run
  // separately in CI.
};

export default nextConfig;
